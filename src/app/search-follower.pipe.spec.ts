import { SearchFollowerPipe } from './search-follower.pipe';

describe('SearchFollowerPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFollowerPipe();
    expect(pipe).toBeTruthy();
  });
});
